<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        label:first-child{
            display: block;
        }
        input{
            padding: 7px 10px;
        }
        form{
            width: 400px;
            background-color: hsl(0deg 0% 100%);
            padding: 30px 30px 30px 50px;
        }
        .inner{
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }
        .main{
            background-color: hsl(0deg 0% 21.18%);
        }
        #card{
            width: 300px;
            margin-bottom: 20px;
        }
        #date, #cvv{
            display: inline;
        }
        #cvv{
            width: 140px;
        }
        .col-2{
            width: 50%;
        }
        .d-flex{
            display: flex;
        }
        button{
            width: 93%;
            padding: 7px;
            border: none;
            margin-top: 30px;
            background-color: hsl(204.2deg 100% 35.49%);
            color: #fff;
            font-weight: bold;
        }
        label{
            font-size: 15px;
            margin-bottom: 5px;
            font-weight: bold;
        }
        img{
            object-fit: cover;
            margin-bottom: 20px;
        }
        .justify-between{
            justify-content: space-between;
        }
    </style>
</head>
<body>

    <div class="main">
        <div class="inner">
            <form id="form">
                <div class="d-flex justify-between">
                    <div>
                        <img src="https://static.vecteezy.com/system/resources/thumbnails/020/975/570/small_2x/visa-logo-visa-icon-transparent-free-png.png" width="100px" height="40px"/>
                    </div>
                    <div>
                        <img src="https://download.logo.wine/logo/Paytm/Paytm-Logo.wine.png" width="100px" height="40px" style="margin-right: 50px;"/>
                    </div>
                </div>
                <div>
                    <label>Card Number</label>
                    <input type="number" id="card"/>
                </div>
                <div class="d-flex">
                    <div class="col-2">
                        <label>Expiry Date</label>
                        <input type="date" id="date"/>
                    </div>
                    <div class="col-2">
                        <label>cvv</label>
                        <input type="number" id="cvv"/>
                    </div>
                </div>
                
        
                <button>Pay Now</button>
            </form>
        </div>
    </div>
    

    <script>
        let form = document.querySelector('#form');

        form.addEventListener('submit', (e)=>{
            e.preventDefault();

            let cardNo = document.querySelector('#card');
            let cvv = document.querySelector('#cvv');
            let expiryDate = document.querySelector('#date');

            fetch("/payment/payments",{
                method:"POST",
                body:JSON.stringify({proName, cvv, date}),
                headers:{
                    "Content-Type": "application/json"
                }
            }).then((res)=> console.log("payment is successfully"))
            .catch((error)=> console.log("something went wrong",error))
        })
    </script>
</body>
</html>